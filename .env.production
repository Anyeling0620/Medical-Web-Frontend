# 生产构建（vite build，mode=production）读取本文件。
# 内容不含密钥，随仓库提交：这样本地构建与 CI（Jenkins 克隆后拿不到被忽略的 .env）产出一致。
#
# 后端地址留空 = 同源相对路径：浏览器请求 /api/v1/*，由线上 nginx 反代到后端容器。
# 不要填绝对地址，否则会绕过同源代理并让 Cookie 登录失效。
VITE_BASE_URL=
# MinIO 对象地址：医生照片等资源由浏览器直连该地址获取。
VITE_MINIO_URL=http://47.104.148.178:9000
VITE_MINIO_BUCKET=hospital
